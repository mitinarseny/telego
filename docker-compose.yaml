version: '3.7'

networks:
  telego:

services:
  bot:
    build:
      context: .
      target: server
      args:
        _project_path: "github.com/mitinarseny/telego"
        _path: "."
    environment:
      TELEGO_BOT_TOKEN: "<your_token_here>"
    networks:
      - telego
